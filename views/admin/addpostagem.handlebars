{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3 >Nova Postagem</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/postagens/nova" method="POST">
            <label for="titulo">Título:</label><br>
            <input type="text" name="titulo" id="titulo" placeholder="Titulo da categoria" class="form-control" >
            <label for="slug">Slug:</label><br>
            <input type="text" name="slug" id="slug" placeholder="Slug da categoria" class="form-control" >
            <label for="descricao">Descrição:</label><br>
            <input type="text" name="descricao" id="descricao" placeholder="Descrição da categoria" class="form-control" >
            <label for="conteudo">Conteúdo:</label><br>
            <input type="text" name="conteudo" id="conteudo" placeholder="conteudo da categoria" class="form-control" >            
            <br>
            <button type="submit" class="btn btn-success">Criar Postagem</button>
        </form>
    </div>
</div>